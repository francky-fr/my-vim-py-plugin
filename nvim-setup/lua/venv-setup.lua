require("venv-selector").setup({
  auto_sources = true, -- Automatically detect virtual environments
  search = true,       -- Search for virtual environments in default locations
  -- search_paths = {
  --   "~/git_rep/my-vim-py-plugin"
  -- },
})

